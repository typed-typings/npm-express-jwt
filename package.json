{
  "name": "express-jwt",
  "version": "1.0.0",
  "private": true,
  "description": "Typings for express-jwt",
  "scripts": {
    "build": "typings bundle -o test/bundle.d.ts",
    "test": "tsc -p test",
    "lint": "tslint -c tslint.json index.d.ts"
  },
  "author": "Felix Becker <felix.b@outlook.com>",
  "license": "ISC",
  "devDependencies": {
    "tslint": "^5.2.0",
    "typescript": "^2.4.2",
    "typings": "^2.1.1"
  }
}
